/*
A falsy (sometimes written falsy) value is a value that is considered false when encountered in a Boolean context. 
*/

// Examples
if (false || null || undefined || 0 || -0 || 0n || NaN || "") {
  console.log("All Are The Examples Of Falsy Values");
}
